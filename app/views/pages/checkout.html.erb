<div class="container-fluid h-100">

  <%= form_for @purchase do |f| %>
  <div class="row">
    <%= @item %>
  </div>
  <%= f.label :first_name %>:
  <%= f.text_field :first_name %>

  <%= f.label :last_name %>:
  <%= f.text_field :last_name %>

  <%= f.submit %>
<% end %>

</div>